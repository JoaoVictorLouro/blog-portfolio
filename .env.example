# Public URL Ghost uses to generate links (must match how you access the site)
URL=http://localhost:2368

# Host port mapped to Ghost's container port 2368
PORT=2368

# Owner display name and site title (created once on first boot)
GHOST_ADMIN_NAME=Admin
GHOST_SITE_TITLE=Kono Gaijin

# Optional YouTube channel for the home-page video strip (public ID, not a secret).
# Omit to disable the strip (API returns an empty video list).
# Sample: @vaantako → UCV3h2_srSVaiEmjrZ8v7Qsw
# YOUTUBE_CHANNEL_ID=UCV3h2_srSVaiEmjrZ8v7Qsw
# YOUTUBE_REFRESH_MS=3600000

# Bootstrap skip flags (default false = run everything). Set true in production
# to avoid overwriting Ghost Admin content on each bootstrap run.
# BOOTSTRAP_SKIP_CONFIGS=false
# BOOTSTRAP_SKIP_TAGS=false
# BOOTSTRAP_SKIP_PAGES=false
# BOOTSTRAP_SKIP_NEWSLETTERS=false
# BOOTSTRAP_SKIP_INTEGRATIONS=false
# BOOTSTRAP_SKIP_ARTICLES=false

# Optional: transactional email (not required for initial admin setup)
# mail__transport=SMTP
# mail__options__host=smtp.example.com
# mail__options__port=587
# mail__options__secure=false
# mail__from=noreply@example.com
