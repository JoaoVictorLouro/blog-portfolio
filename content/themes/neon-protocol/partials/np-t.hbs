{{!-- AUTO-GENERATED by scripts/build-theme-i18n.mjs — do not edit --}}
{{#match key "% min read"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}% MIN READ{{else has tag="#lang-pt-br"}}% MIN READ{{else has tag="#lang-es-la"}}% MIN READ{{else}}% MIN READ{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}% MIN READ{{else match (block "np_lang") "ja-jp"}}% MIN READ{{else match (block "np_lang") "pt-br"}}% MIN READ{{else match (block "np_lang") "es-la"}}% MIN READ{{else}}% MIN READ{{/match}}
  {{/is}}
{{/match}}
{{#match key "1 min read"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}1 MIN READ{{else has tag="#lang-pt-br"}}1 MIN READ{{else has tag="#lang-es-la"}}1 MIN READ{{else}}1 MIN READ{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}1 MIN READ{{else match (block "np_lang") "ja-jp"}}1 MIN READ{{else match (block "np_lang") "pt-br"}}1 MIN READ{{else match (block "np_lang") "es-la"}}1 MIN READ{{else}}1 MIN READ{{/match}}
  {{/is}}
{{/match}}
{{#match key "ABOUT"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}概要{{else has tag="#lang-pt-br"}}SOBRE{{else has tag="#lang-es-la"}}ACERCA{{else}}ABOUT{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}ABOUT{{else match (block "np_lang") "ja-jp"}}概要{{else match (block "np_lang") "pt-br"}}SOBRE{{else match (block "np_lang") "es-la"}}ACERCA{{else}}ABOUT{{/match}}
  {{/is}}
{{/match}}
{{#match key "ABOUT_STATUS"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}ステータス: オンライン // 位置: 未定義{{else has tag="#lang-pt-br"}}STATUS: ONLINE // LOCAL: INDEFINIDO{{else has tag="#lang-es-la"}}ESTADO: EN LÍNEA // UBICACIÓN: INDEFINIDA{{else}}STATUS: ONLINE // LOCATION: UNDEFINED{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}STATUS: ONLINE // LOCATION: UNDEFINED{{else match (block "np_lang") "ja-jp"}}ステータス: オンライン // 位置: 未定義{{else match (block "np_lang") "pt-br"}}STATUS: ONLINE // LOCAL: INDEFINIDO{{else match (block "np_lang") "es-la"}}ESTADO: EN LÍNEA // UBICACIÓN: INDEFINIDA{{else}}STATUS: ONLINE // LOCATION: UNDEFINED{{/match}}
  {{/is}}
{{/match}}
{{#match key "ALL"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}すべて{{else has tag="#lang-pt-br"}}TODOS{{else has tag="#lang-es-la"}}TODOS{{else}}ALL{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}ALL{{else match (block "np_lang") "ja-jp"}}すべて{{else match (block "np_lang") "pt-br"}}TODOS{{else match (block "np_lang") "es-la"}}TODOS{{else}}ALL{{/match}}
  {{/is}}
{{/match}}
{{#match key "ALL RIGHTS RESERVED"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}ALL RIGHTS RESERVED{{else has tag="#lang-pt-br"}}ALL RIGHTS RESERVED{{else has tag="#lang-es-la"}}ALL RIGHTS RESERVED{{else}}ALL RIGHTS RESERVED{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}ALL RIGHTS RESERVED{{else match (block "np_lang") "ja-jp"}}ALL RIGHTS RESERVED{{else match (block "np_lang") "pt-br"}}ALL RIGHTS RESERVED{{else match (block "np_lang") "es-la"}}ALL RIGHTS RESERVED{{else}}ALL RIGHTS RESERVED{{/match}}
  {{/is}}
{{/match}}
{{#match key "ALREADY_LINKED?"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}ALREADY_LINKED?{{else has tag="#lang-pt-br"}}ALREADY_LINKED?{{else has tag="#lang-es-la"}}ALREADY_LINKED?{{else}}ALREADY_LINKED?{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}ALREADY_LINKED?{{else match (block "np_lang") "ja-jp"}}ALREADY_LINKED?{{else match (block "np_lang") "pt-br"}}ALREADY_LINKED?{{else match (block "np_lang") "es-la"}}ALREADY_LINKED?{{else}}ALREADY_LINKED?{{/match}}
  {{/is}}
{{/match}}
{{#match key "ARTICLES"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}記事{{else has tag="#lang-pt-br"}}ARTIGOS{{else has tag="#lang-es-la"}}ARTÍCULOS{{else}}ARTICLES{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}ARTICLES{{else match (block "np_lang") "ja-jp"}}記事{{else match (block "np_lang") "pt-br"}}ARTIGOS{{else match (block "np_lang") "es-la"}}ARTÍCULOS{{else}}ARTICLES{{/match}}
  {{/is}}
{{/match}}
{{#match key "ARTICLES_TITLE"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}記事{{else has tag="#lang-pt-br"}}ARTIGOS{{else has tag="#lang-es-la"}}ARTÍCULOS{{else}}ARTICLES{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}ARTICLES{{else match (block "np_lang") "ja-jp"}}記事{{else match (block "np_lang") "pt-br"}}ARTIGOS{{else match (block "np_lang") "es-la"}}ARTÍCULOS{{else}}ARTICLES{{/match}}
  {{/is}}
{{/match}}
{{#match key "AUTHENTICATE"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}AUTHENTICATE{{else has tag="#lang-pt-br"}}AUTHENTICATE{{else has tag="#lang-es-la"}}AUTHENTICATE{{else}}AUTHENTICATE{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}AUTHENTICATE{{else match (block "np_lang") "ja-jp"}}AUTHENTICATE{{else match (block "np_lang") "pt-br"}}AUTHENTICATE{{else match (block "np_lang") "es-la"}}AUTHENTICATE{{else}}AUTHENTICATE{{/match}}
  {{/is}}
{{/match}}
{{#match key "AUTHENTICATE_NODE"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}AUTHENTICATE_NODE{{else has tag="#lang-pt-br"}}AUTHENTICATE_NODE{{else has tag="#lang-es-la"}}AUTHENTICATE_NODE{{else}}AUTHENTICATE_NODE{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}AUTHENTICATE_NODE{{else match (block "np_lang") "ja-jp"}}AUTHENTICATE_NODE{{else match (block "np_lang") "pt-br"}}AUTHENTICATE_NODE{{else match (block "np_lang") "es-la"}}AUTHENTICATE_NODE{{else}}AUTHENTICATE_NODE{{/match}}
  {{/is}}
{{/match}}
{{#match key "AUTHOR"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}著者{{else has tag="#lang-pt-br"}}AUTOR{{else has tag="#lang-es-la"}}AUTOR{{else}}AUTHOR{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}AUTHOR{{else match (block "np_lang") "ja-jp"}}著者{{else match (block "np_lang") "pt-br"}}AUTOR{{else match (block "np_lang") "es-la"}}AUTOR{{else}}AUTHOR{{/match}}
  {{/is}}
{{/match}}
{{#match key "AWAITING_TRANSMISSION"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}AWAITING_TRANSMISSION...{{else has tag="#lang-pt-br"}}AWAITING_TRANSMISSION...{{else has tag="#lang-es-la"}}AWAITING_TRANSMISSION...{{else}}AWAITING_TRANSMISSION...{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}AWAITING_TRANSMISSION...{{else match (block "np_lang") "ja-jp"}}AWAITING_TRANSMISSION...{{else match (block "np_lang") "pt-br"}}AWAITING_TRANSMISSION...{{else match (block "np_lang") "es-la"}}AWAITING_TRANSMISSION...{{else}}AWAITING_TRANSMISSION...{{/match}}
  {{/is}}
{{/match}}
{{#match key "BANDWIDTH_OPTIMAL"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}BANDWIDTH: OPTIMAL{{else has tag="#lang-pt-br"}}BANDWIDTH: OPTIMAL{{else has tag="#lang-es-la"}}BANDWIDTH: OPTIMAL{{else}}BANDWIDTH: OPTIMAL{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}BANDWIDTH: OPTIMAL{{else match (block "np_lang") "ja-jp"}}BANDWIDTH: OPTIMAL{{else match (block "np_lang") "pt-br"}}BANDWIDTH: OPTIMAL{{else match (block "np_lang") "es-la"}}BANDWIDTH: OPTIMAL{{else}}BANDWIDTH: OPTIMAL{{/match}}
  {{/is}}
{{/match}}
{{#match key "Back"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}戻る{{else has tag="#lang-pt-br"}}VOLTAR{{else has tag="#lang-es-la"}}ATRÁS{{else}}BACK{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}BACK{{else match (block "np_lang") "ja-jp"}}戻る{{else match (block "np_lang") "pt-br"}}VOLTAR{{else match (block "np_lang") "es-la"}}ATRÁS{{else}}BACK{{/match}}
  {{/is}}
{{/match}}
{{#match key "CHANNEL_OPEN // CHECK_COMM_LINK"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}CHANNEL_OPEN // CHECK_COMM_LINK{{else has tag="#lang-pt-br"}}CHANNEL_OPEN // CHECK_COMM_LINK{{else has tag="#lang-es-la"}}CHANNEL_OPEN // CHECK_COMM_LINK{{else}}CHANNEL_OPEN // CHECK_COMM_LINK{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}CHANNEL_OPEN // CHECK_COMM_LINK{{else match (block "np_lang") "ja-jp"}}CHANNEL_OPEN // CHECK_COMM_LINK{{else match (block "np_lang") "pt-br"}}CHANNEL_OPEN // CHECK_COMM_LINK{{else match (block "np_lang") "es-la"}}CHANNEL_OPEN // CHECK_COMM_LINK{{else}}CHANNEL_OPEN // CHECK_COMM_LINK{{/match}}
  {{/is}}
{{/match}}
{{#match key "CLEARANCE"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}CLEARANCE{{else has tag="#lang-pt-br"}}CLEARANCE{{else has tag="#lang-es-la"}}CLEARANCE{{else}}CLEARANCE{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}CLEARANCE{{else match (block "np_lang") "ja-jp"}}CLEARANCE{{else match (block "np_lang") "pt-br"}}CLEARANCE{{else match (block "np_lang") "es-la"}}CLEARANCE{{else}}CLEARANCE{{/match}}
  {{/is}}
{{/match}}
{{#match key "COMM_LINK"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}COMM_LINK{{else has tag="#lang-pt-br"}}COMM_LINK{{else has tag="#lang-es-la"}}COMM_LINK{{else}}COMM_LINK{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}COMM_LINK{{else match (block "np_lang") "ja-jp"}}COMM_LINK{{else match (block "np_lang") "pt-br"}}COMM_LINK{{else match (block "np_lang") "es-la"}}COMM_LINK{{else}}COMM_LINK{{/match}}
  {{/is}}
{{/match}}
{{#match key "Close"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}CLOSE{{else has tag="#lang-pt-br"}}CLOSE{{else has tag="#lang-es-la"}}CLOSE{{else}}CLOSE{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}CLOSE{{else match (block "np_lang") "ja-jp"}}CLOSE{{else match (block "np_lang") "pt-br"}}CLOSE{{else match (block "np_lang") "es-la"}}CLOSE{{else}}CLOSE{{/match}}
  {{/is}}
{{/match}}
{{#match key "Comm_Link_ID [Email]"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}Comm_Link_ID [Email]{{else has tag="#lang-pt-br"}}Comm_Link_ID [Email]{{else has tag="#lang-es-la"}}Comm_Link_ID [Email]{{else}}Comm_Link_ID [Email]{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}Comm_Link_ID [Email]{{else match (block "np_lang") "ja-jp"}}Comm_Link_ID [Email]{{else match (block "np_lang") "pt-br"}}Comm_Link_ID [Email]{{else match (block "np_lang") "es-la"}}Comm_Link_ID [Email]{{else}}Comm_Link_ID [Email]{{/match}}
  {{/is}}
{{/match}}
{{#match key "Comments"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}BBS_CONNECTION{{else has tag="#lang-pt-br"}}BBS_CONNECTION{{else has tag="#lang-es-la"}}BBS_CONNECTION{{else}}BBS_CONNECTION{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}BBS_CONNECTION{{else match (block "np_lang") "ja-jp"}}BBS_CONNECTION{{else match (block "np_lang") "pt-br"}}BBS_CONNECTION{{else match (block "np_lang") "es-la"}}BBS_CONNECTION{{else}}BBS_CONNECTION{{/match}}
  {{/is}}
{{/match}}
{{#match key "Copy link"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}Copy link{{else has tag="#lang-pt-br"}}Copy link{{else has tag="#lang-es-la"}}Copy link{{else}}Copy link{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}Copy link{{else match (block "np_lang") "ja-jp"}}Copy link{{else match (block "np_lang") "pt-br"}}Copy link{{else match (block "np_lang") "es-la"}}Copy link{{else}}Copy link{{/match}}
  {{/is}}
{{/match}}
{{#match key "DATA_MAP"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}DATA_MAP{{else has tag="#lang-pt-br"}}DATA_MAP{{else has tag="#lang-es-la"}}DATA_MAP{{else}}DATA_MAP{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}DATA_MAP{{else match (block "np_lang") "ja-jp"}}DATA_MAP{{else match (block "np_lang") "pt-br"}}DATA_MAP{{else match (block "np_lang") "es-la"}}DATA_MAP{{else}}DATA_MAP{{/match}}
  {{/is}}
{{/match}}
{{#match key "DISCONNECT"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}DISCONNECT{{else has tag="#lang-pt-br"}}DISCONNECT{{else has tag="#lang-es-la"}}DISCONNECT{{else}}DISCONNECT{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}DISCONNECT{{else match (block "np_lang") "ja-jp"}}DISCONNECT{{else match (block "np_lang") "pt-br"}}DISCONNECT{{else match (block "np_lang") "es-la"}}DISCONNECT{{else}}DISCONNECT{{/match}}
  {{/is}}
{{/match}}
{{#match key "EMAIL_LABEL"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}Comm_Link_ID [Email]{{else has tag="#lang-pt-br"}}Comm_Link_ID [Email]{{else has tag="#lang-es-la"}}Comm_Link_ID [Email]{{else}}Comm_Link_ID [Email]{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}Comm_Link_ID [Email]{{else match (block "np_lang") "ja-jp"}}Comm_Link_ID [Email]{{else match (block "np_lang") "pt-br"}}Comm_Link_ID [Email]{{else match (block "np_lang") "es-la"}}Comm_Link_ID [Email]{{else}}Comm_Link_ID [Email]{{/match}}
  {{/is}}
{{/match}}
{{#match key "EMAIL_PLACEHOLDER"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}user@network.node{{else has tag="#lang-pt-br"}}user@network.node{{else has tag="#lang-es-la"}}user@network.node{{else}}user@network.node{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}user@network.node{{else match (block "np_lang") "ja-jp"}}user@network.node{{else match (block "np_lang") "pt-br"}}user@network.node{{else match (block "np_lang") "es-la"}}user@network.node{{else}}user@network.node{{/match}}
  {{/is}}
{{/match}}
{{#match key "ENCRYPTED"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}暗号化済み{{else has tag="#lang-pt-br"}}CRIPTOGRAFADO{{else has tag="#lang-es-la"}}CIFRADO{{else}}ENCRYPTED{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}ENCRYPTED{{else match (block "np_lang") "ja-jp"}}暗号化済み{{else match (block "np_lang") "pt-br"}}CRIPTOGRAFADO{{else match (block "np_lang") "es-la"}}CIFRADO{{else}}ENCRYPTED{{/match}}
  {{/is}}
{{/match}}
{{#match key "ENCRYPTION_LOG"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}ENCRYPTION_LOG{{else has tag="#lang-pt-br"}}ENCRYPTION_LOG{{else has tag="#lang-es-la"}}ENCRYPTION_LOG{{else}}ENCRYPTION_LOG{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}ENCRYPTION_LOG{{else match (block "np_lang") "ja-jp"}}ENCRYPTION_LOG{{else match (block "np_lang") "pt-br"}}ENCRYPTION_LOG{{else match (block "np_lang") "es-la"}}ENCRYPTION_LOG{{else}}ENCRYPTION_LOG{{/match}}
  {{/is}}
{{/match}}
{{#match key "ENTER_ADDRESS"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}ENTER_ADDRESS{{else has tag="#lang-pt-br"}}ENTER_ADDRESS{{else has tag="#lang-es-la"}}ENTER_ADDRESS{{else}}ENTER_ADDRESS{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}ENTER_ADDRESS{{else match (block "np_lang") "ja-jp"}}ENTER_ADDRESS{{else match (block "np_lang") "pt-br"}}ENTER_ADDRESS{{else match (block "np_lang") "es-la"}}ENTER_ADDRESS{{else}}ENTER_ADDRESS{{/match}}
  {{/is}}
{{/match}}
{{#match key "ESTABLISH_CONNECTION"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}接続を確立{{else has tag="#lang-pt-br"}}ESTABELECER_CONEXÃO{{else has tag="#lang-es-la"}}ESTABLECER_CONEXIÓN{{else}}ESTABLISH_CONNECTION{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}ESTABLISH_CONNECTION{{else match (block "np_lang") "ja-jp"}}接続を確立{{else match (block "np_lang") "pt-br"}}ESTABELECER_CONEXÃO{{else match (block "np_lang") "es-la"}}ESTABLECER_CONEXIÓN{{else}}ESTABLISH_CONNECTION{{/match}}
  {{/is}}
{{/match}}
{{#match key "FILTER"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}フィルター{{else has tag="#lang-pt-br"}}FILTRAR{{else has tag="#lang-es-la"}}FILTRAR{{else}}FILTER{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}FILTER{{else match (block "np_lang") "ja-jp"}}フィルター{{else match (block "np_lang") "pt-br"}}FILTRAR{{else match (block "np_lang") "es-la"}}FILTRAR{{else}}FILTER{{/match}}
  {{/is}}
{{/match}}
{{#match key "FREE_FEED"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}FREE_FEED{{else has tag="#lang-pt-br"}}FREE_FEED{{else has tag="#lang-es-la"}}FREE_FEED{{else}}FREE_FEED{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}FREE_FEED{{else match (block "np_lang") "ja-jp"}}FREE_FEED{{else match (block "np_lang") "pt-br"}}FREE_FEED{{else match (block "np_lang") "es-la"}}FREE_FEED{{else}}FREE_FEED{{/match}}
  {{/is}}
{{/match}}
{{#match key "GITHUB_REPO"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}GitHub_Repo{{else has tag="#lang-pt-br"}}GitHub_Repo{{else has tag="#lang-es-la"}}GitHub_Repo{{else}}GitHub_Repo{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}GitHub_Repo{{else match (block "np_lang") "ja-jp"}}GitHub_Repo{{else match (block "np_lang") "pt-br"}}GitHub_Repo{{else match (block "np_lang") "es-la"}}GitHub_Repo{{else}}GitHub_Repo{{/match}}
  {{/is}}
{{/match}}
{{#match key "HANDLE"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}HANDLE{{else has tag="#lang-pt-br"}}HANDLE{{else has tag="#lang-es-la"}}HANDLE{{else}}HANDLE{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}HANDLE{{else match (block "np_lang") "ja-jp"}}HANDLE{{else match (block "np_lang") "pt-br"}}HANDLE{{else match (block "np_lang") "es-la"}}HANDLE{{else}}HANDLE{{/match}}
  {{/is}}
{{/match}}
{{#match key "HOME"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}ホーム{{else has tag="#lang-pt-br"}}INÍCIO{{else has tag="#lang-es-la"}}INICIO{{else}}HOME{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}HOME{{else match (block "np_lang") "ja-jp"}}ホーム{{else match (block "np_lang") "pt-br"}}INÍCIO{{else match (block "np_lang") "es-la"}}INICIO{{else}}HOME{{/match}}
  {{/is}}
{{/match}}
{{#match key "INITIATE_SEQUENCE"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}シーケンス開始{{else has tag="#lang-pt-br"}}INICIAR_SEQUÊNCIA{{else has tag="#lang-es-la"}}INICIAR_SECUENCIA{{else}}INITIATE_SEQUENCE{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}INITIATE_SEQUENCE{{else match (block "np_lang") "ja-jp"}}シーケンス開始{{else match (block "np_lang") "pt-br"}}INICIAR_SEQUÊNCIA{{else match (block "np_lang") "es-la"}}INICIAR_SECUENCIA{{else}}INITIATE_SEQUENCE{{/match}}
  {{/is}}
{{/match}}
{{#match key "LANGUAGE"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}言語{{else has tag="#lang-pt-br"}}Idioma{{else has tag="#lang-es-la"}}Idioma{{else}}Language{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}Language{{else match (block "np_lang") "ja-jp"}}言語{{else match (block "np_lang") "pt-br"}}Idioma{{else match (block "np_lang") "es-la"}}Idioma{{else}}Language{{/match}}
  {{/is}}
{{/match}}
{{#match key "LATEST_TRANSMISSIONS"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}最新の配信 // フィード同期{{else has tag="#lang-pt-br"}}ÚLTIMAS TRANSMISSÕES // SYNC DO FEED{{else has tag="#lang-es-la"}}ÚLTIMAS TRANSMISIONES // SYNC DEL FEED{{else}}LATEST TRANSMISSIONS // FEED SYNC{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}LATEST TRANSMISSIONS // FEED SYNC{{else match (block "np_lang") "ja-jp"}}最新の配信 // フィード同期{{else match (block "np_lang") "pt-br"}}ÚLTIMAS TRANSMISSÕES // SYNC DO FEED{{else match (block "np_lang") "es-la"}}ÚLTIMAS TRANSMISIONES // SYNC DEL FEED{{else}}LATEST TRANSMISSIONS // FEED SYNC{{/match}}
  {{/is}}
{{/match}}
{{#match key "LIVE_FEED"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}ライブフィード{{else has tag="#lang-pt-br"}}FEED AO VIVO{{else has tag="#lang-es-la"}}FEED EN VIVO{{else}}LIVE FEED{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}LIVE FEED{{else match (block "np_lang") "ja-jp"}}ライブフィード{{else match (block "np_lang") "pt-br"}}FEED AO VIVO{{else match (block "np_lang") "es-la"}}FEED EN VIVO{{else}}LIVE FEED{{/match}}
  {{/is}}
{{/match}}
{{#match key "LOG_DECRYPTED"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}LOG_DECRYPTED{{else has tag="#lang-pt-br"}}LOG_DECRYPTED{{else has tag="#lang-es-la"}}LOG_DECRYPTED{{else}}LOG_DECRYPTED{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}LOG_DECRYPTED{{else match (block "np_lang") "ja-jp"}}LOG_DECRYPTED{{else match (block "np_lang") "pt-br"}}LOG_DECRYPTED{{else match (block "np_lang") "es-la"}}LOG_DECRYPTED{{else}}LOG_DECRYPTED{{/match}}
  {{/is}}
{{/match}}
{{#match key "Load more"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}LOAD_MORE_DATA{{else has tag="#lang-pt-br"}}LOAD_MORE_DATA{{else has tag="#lang-es-la"}}LOAD_MORE_DATA{{else}}LOAD_MORE_DATA{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}LOAD_MORE_DATA{{else match (block "np_lang") "ja-jp"}}LOAD_MORE_DATA{{else match (block "np_lang") "pt-br"}}LOAD_MORE_DATA{{else match (block "np_lang") "es-la"}}LOAD_MORE_DATA{{else}}LOAD_MORE_DATA{{/match}}
  {{/is}}
{{/match}}
{{#match key "MESSAGE_LABEL"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}Data_Payload [Message]{{else has tag="#lang-pt-br"}}Data_Payload [Message]{{else has tag="#lang-es-la"}}Data_Payload [Message]{{else}}Data_Payload [Message]{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}Data_Payload [Message]{{else match (block "np_lang") "ja-jp"}}Data_Payload [Message]{{else match (block "np_lang") "pt-br"}}Data_Payload [Message]{{else match (block "np_lang") "es-la"}}Data_Payload [Message]{{else}}Data_Payload [Message]{{/match}}
  {{/is}}
{{/match}}
{{#match key "MESSAGE_PLACEHOLDER"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}Enter transmission...{{else has tag="#lang-pt-br"}}Enter transmission...{{else has tag="#lang-es-la"}}Enter transmission...{{else}}Enter transmission...{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}Enter transmission...{{else match (block "np_lang") "ja-jp"}}Enter transmission...{{else match (block "np_lang") "pt-br"}}Enter transmission...{{else match (block "np_lang") "es-la"}}Enter transmission...{{else}}Enter transmission...{{/match}}
  {{/is}}
{{/match}}
{{#match key "Menu"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}メニュー{{else has tag="#lang-pt-br"}}Menu{{else has tag="#lang-es-la"}}Menú{{else}}Menu{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}Menu{{else match (block "np_lang") "ja-jp"}}メニュー{{else match (block "np_lang") "pt-br"}}Menu{{else match (block "np_lang") "es-la"}}Menú{{else}}Menu{{/match}}
  {{/is}}
{{/match}}
{{#match key "NETWORK_NODES"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}ネットワークノード{{else has tag="#lang-pt-br"}}NÓS_DA_REDE{{else has tag="#lang-es-la"}}NODOS_DE_RED{{else}}NETWORK_NODES{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}NETWORK_NODES{{else match (block "np_lang") "ja-jp"}}ネットワークノード{{else match (block "np_lang") "pt-br"}}NÓS_DA_REDE{{else match (block "np_lang") "es-la"}}NODOS_DE_RED{{else}}NETWORK_NODES{{/match}}
  {{/is}}
{{/match}}
{{#match key "NEW_NODE?"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}NEW_NODE?{{else has tag="#lang-pt-br"}}NEW_NODE?{{else has tag="#lang-es-la"}}NEW_NODE?{{else}}NEW_NODE?{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}NEW_NODE?{{else match (block "np_lang") "ja-jp"}}NEW_NODE?{{else match (block "np_lang") "pt-br"}}NEW_NODE?{{else match (block "np_lang") "es-la"}}NEW_NODE?{{else}}NEW_NODE?{{/match}}
  {{/is}}
{{/match}}
{{#match key "NODE_SESSION"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}NODE_SESSION{{else has tag="#lang-pt-br"}}NODE_SESSION{{else has tag="#lang-es-la"}}NODE_SESSION{{else}}NODE_SESSION{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}NODE_SESSION{{else match (block "np_lang") "ja-jp"}}NODE_SESSION{{else match (block "np_lang") "pt-br"}}NODE_SESSION{{else match (block "np_lang") "es-la"}}NODE_SESSION{{else}}NODE_SESSION{{/match}}
  {{/is}}
{{/match}}
{{#match key "NODE_SYNC_COMPLETE"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}NODE_SYNC_COMPLETE{{else has tag="#lang-pt-br"}}NODE_SYNC_COMPLETE{{else has tag="#lang-es-la"}}NODE_SYNC_COMPLETE{{else}}NODE_SYNC_COMPLETE{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}NODE_SYNC_COMPLETE{{else match (block "np_lang") "ja-jp"}}NODE_SYNC_COMPLETE{{else match (block "np_lang") "pt-br"}}NODE_SYNC_COMPLETE{{else match (block "np_lang") "es-la"}}NODE_SYNC_COMPLETE{{else}}NODE_SYNC_COMPLETE{{/match}}
  {{/is}}
{{/match}}
{{#match key "NO_TRANSLATIONS"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}他の言語版はありません。{{else has tag="#lang-pt-br"}}Nenhuma outra versão de idioma disponível.{{else has tag="#lang-es-la"}}No hay otras versiones de idioma disponibles.{{else}}No other language versions available.{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}No other language versions available.{{else match (block "np_lang") "ja-jp"}}他の言語版はありません。{{else match (block "np_lang") "pt-br"}}Nenhuma outra versão de idioma disponível.{{else match (block "np_lang") "es-la"}}No hay otras versiones de idioma disponibles.{{else}}No other language versions available.{{/match}}
  {{/is}}
{{/match}}
{{#match key "Newer posts"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}PREV_PAGE{{else has tag="#lang-pt-br"}}PREV_PAGE{{else has tag="#lang-es-la"}}PREV_PAGE{{else}}PREV_PAGE{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}PREV_PAGE{{else match (block "np_lang") "ja-jp"}}PREV_PAGE{{else match (block "np_lang") "pt-br"}}PREV_PAGE{{else match (block "np_lang") "es-la"}}PREV_PAGE{{else}}PREV_PAGE{{/match}}
  {{/is}}
{{/match}}
{{#match key "Next"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}次へ{{else has tag="#lang-pt-br"}}PRÓXIMO{{else has tag="#lang-es-la"}}SIGUIENTE{{else}}NEXT{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}NEXT{{else match (block "np_lang") "ja-jp"}}次へ{{else match (block "np_lang") "pt-br"}}PRÓXIMO{{else match (block "np_lang") "es-la"}}SIGUIENTE{{else}}NEXT{{/match}}
  {{/is}}
{{/match}}
{{#match key "No posts"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}NO_SIGNAL{{else has tag="#lang-pt-br"}}NO_SIGNAL{{else has tag="#lang-es-la"}}NO_SIGNAL{{else}}NO_SIGNAL{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}NO_SIGNAL{{else match (block "np_lang") "ja-jp"}}NO_SIGNAL{{else match (block "np_lang") "pt-br"}}NO_SIGNAL{{else match (block "np_lang") "es-la"}}NO_SIGNAL{{else}}NO_SIGNAL{{/match}}
  {{/is}}
{{/match}}
{{#match key "OPEN_CHANNEL"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}OPEN_CHANNEL{{else has tag="#lang-pt-br"}}OPEN_CHANNEL{{else has tag="#lang-es-la"}}OPEN_CHANNEL{{else}}OPEN_CHANNEL{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}OPEN_CHANNEL{{else match (block "np_lang") "ja-jp"}}OPEN_CHANNEL{{else match (block "np_lang") "pt-br"}}OPEN_CHANNEL{{else match (block "np_lang") "es-la"}}OPEN_CHANNEL{{else}}OPEN_CHANNEL{{/match}}
  {{/is}}
{{/match}}
{{#match key "Older posts"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}NEXT_PAGE{{else has tag="#lang-pt-br"}}NEXT_PAGE{{else has tag="#lang-es-la"}}NEXT_PAGE{{else}}NEXT_PAGE{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}NEXT_PAGE{{else match (block "np_lang") "ja-jp"}}NEXT_PAGE{{else match (block "np_lang") "pt-br"}}NEXT_PAGE{{else match (block "np_lang") "es-la"}}NEXT_PAGE{{else}}NEXT_PAGE{{/match}}
  {{/is}}
{{/match}}
{{#match key "PORTFOLIO"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}ポートフォリオ{{else has tag="#lang-pt-br"}}PORTFÓLIO{{else has tag="#lang-es-la"}}PORTAFOLIO{{else}}PORTFOLIO{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}PORTFOLIO{{else match (block "np_lang") "ja-jp"}}ポートフォリオ{{else match (block "np_lang") "pt-br"}}PORTFÓLIO{{else match (block "np_lang") "es-la"}}PORTAFOLIO{{else}}PORTFOLIO{{/match}}
  {{/is}}
{{/match}}
{{#match key "PORTFOLIO_SUBTITLE"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}暗号化されたビジュアルログ。アクセスレベル: Alpha。{{else has tag="#lang-pt-br"}}Logs visuais criptografados. Nível de acesso: Alpha.{{else has tag="#lang-es-la"}}Registros visuales cifrados. Nivel de acceso: Alpha.{{else}}Encrypted visual logs. Access level: Alpha.{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}Encrypted visual logs. Access level: Alpha.{{else match (block "np_lang") "ja-jp"}}暗号化されたビジュアルログ。アクセスレベル: Alpha。{{else match (block "np_lang") "pt-br"}}Logs visuais criptografados. Nível de acesso: Alpha.{{else match (block "np_lang") "es-la"}}Registros visuales cifrados. Nivel de acceso: Alpha.{{else}}Encrypted visual logs. Access level: Alpha.{{/match}}
  {{/is}}
{{/match}}
{{#match key "PORTFOLIO_TITLE"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}ポートフォリオ{{else has tag="#lang-pt-br"}}PORTFÓLIO{{else has tag="#lang-es-la"}}PORTAFOLIO{{else}}PORTFOLIO{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}PORTFOLIO{{else match (block "np_lang") "ja-jp"}}ポートフォリオ{{else match (block "np_lang") "pt-br"}}PORTFÓLIO{{else match (block "np_lang") "es-la"}}PORTAFOLIO{{else}}PORTFOLIO{{/match}}
  {{/is}}
{{/match}}
{{#match key "Page {page} of {pages}"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}PAGE {page} / {pages}{{else has tag="#lang-pt-br"}}PÁGINA {page} / {pages}{{else has tag="#lang-es-la"}}PÁGINA {page} / {pages}{{else}}PAGE {page} / {pages}{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}PAGE {page} / {pages}{{else match (block "np_lang") "ja-jp"}}PAGE {page} / {pages}{{else match (block "np_lang") "pt-br"}}PÁGINA {page} / {pages}{{else match (block "np_lang") "es-la"}}PÁGINA {page} / {pages}{{else}}PAGE {page} / {pages}{{/match}}
  {{/is}}
{{/match}}
{{#match key "Previous"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}PREV{{else has tag="#lang-pt-br"}}PREV{{else has tag="#lang-es-la"}}PREV{{else}}PREV{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}PREV{{else match (block "np_lang") "ja-jp"}}PREV{{else match (block "np_lang") "pt-br"}}PREV{{else match (block "np_lang") "es-la"}}PREV{{else}}PREV{{/match}}
  {{/is}}
{{/match}}
{{#match key "READING_PACKET_DATA"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}READING_PACKET_DATA...{{else has tag="#lang-pt-br"}}READING_PACKET_DATA...{{else has tag="#lang-es-la"}}READING_PACKET_DATA...{{else}}READING_PACKET_DATA...{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}READING_PACKET_DATA...{{else match (block "np_lang") "ja-jp"}}READING_PACKET_DATA...{{else match (block "np_lang") "pt-br"}}READING_PACKET_DATA...{{else match (block "np_lang") "es-la"}}READING_PACKET_DATA...{{else}}READING_PACKET_DATA...{{/match}}
  {{/is}}
{{/match}}
{{#match key "READ_IN_ANOTHER_LANGUAGE"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}他の言語で読む{{else has tag="#lang-pt-br"}}LER EM OUTRO IDIOMA{{else has tag="#lang-es-la"}}LEER EN OTRO IDIOMA{{else}}READ IN ANOTHER LANGUAGE{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}READ IN ANOTHER LANGUAGE{{else match (block "np_lang") "ja-jp"}}他の言語で読む{{else match (block "np_lang") "pt-br"}}LER EM OUTRO IDIOMA{{else match (block "np_lang") "es-la"}}LEER EN OTRO IDIOMA{{else}}READ IN ANOTHER LANGUAGE{{/match}}
  {{/is}}
{{/match}}
{{#match key "RECENT_ARTICLES"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}最近の記事{{else has tag="#lang-pt-br"}}ARTIGOS RECENTES{{else has tag="#lang-es-la"}}ARTÍCULOS RECIENTES{{else}}RECENT_ARTICLES{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}RECENT_ARTICLES{{else match (block "np_lang") "ja-jp"}}最近の記事{{else match (block "np_lang") "pt-br"}}ARTIGOS RECENTES{{else match (block "np_lang") "es-la"}}ARTÍCULOS RECIENTES{{else}}RECENT_ARTICLES{{/match}}
  {{/is}}
{{/match}}
{{#match key "REQUEST_TOKEN"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}REQUEST_TOKEN{{else has tag="#lang-pt-br"}}REQUEST_TOKEN{{else has tag="#lang-es-la"}}REQUEST_TOKEN{{else}}REQUEST_TOKEN{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}REQUEST_TOKEN{{else match (block "np_lang") "ja-jp"}}REQUEST_TOKEN{{else match (block "np_lang") "pt-br"}}REQUEST_TOKEN{{else match (block "np_lang") "es-la"}}REQUEST_TOKEN{{else}}REQUEST_TOKEN{{/match}}
  {{/is}}
{{/match}}
{{#match key "Receive encrypted drops directly to your local node."}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}暗号化された配信をローカルノードに直接受信。{{else has tag="#lang-pt-br"}}Receba transmissões criptografadas diretamente no seu nó local.{{else has tag="#lang-es-la"}}Recibe transmisiones cifradas directamente en tu nodo local.{{else}}Receive encrypted drops directly to your local node.{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}Receive encrypted drops directly to your local node.{{else match (block "np_lang") "ja-jp"}}暗号化された配信をローカルノードに直接受信。{{else match (block "np_lang") "pt-br"}}Receba transmissões criptografadas diretamente no seu nó local.{{else match (block "np_lang") "es-la"}}Recibe transmisiones cifradas directamente en tu nodo local.{{else}}Receive encrypted drops directly to your local node.{{/match}}
  {{/is}}
{{/match}}
{{#match key "Request a one-time access token for this node."}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}Request a one-time access token for this node.{{else has tag="#lang-pt-br"}}Request a one-time access token for this node.{{else has tag="#lang-es-la"}}Request a one-time access token for this node.{{else}}Request a one-time access token for this node.{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}Request a one-time access token for this node.{{else match (block "np_lang") "ja-jp"}}Request a one-time access token for this node.{{else match (block "np_lang") "pt-br"}}Request a one-time access token for this node.{{else match (block "np_lang") "es-la"}}Request a one-time access token for this node.{{else}}Request a one-time access token for this node.{{/match}}
  {{/is}}
{{/match}}
{{#match key "SECURE_CHANNEL"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}SECURE_CHANNEL{{else has tag="#lang-pt-br"}}SECURE_CHANNEL{{else has tag="#lang-es-la"}}SECURE_CHANNEL{{else}}SECURE_CHANNEL{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}SECURE_CHANNEL{{else match (block "np_lang") "ja-jp"}}SECURE_CHANNEL{{else match (block "np_lang") "pt-br"}}SECURE_CHANNEL{{else match (block "np_lang") "es-la"}}SECURE_CHANNEL{{else}}SECURE_CHANNEL{{/match}}
  {{/is}}
{{/match}}
{{#match key "SELECT_LANGUAGE"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}言語を選択{{else has tag="#lang-pt-br"}}Selecionar idioma{{else has tag="#lang-es-la"}}Seleccionar idioma{{else}}Select language{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}Select language{{else match (block "np_lang") "ja-jp"}}言語を選択{{else match (block "np_lang") "pt-br"}}Selecionar idioma{{else match (block "np_lang") "es-la"}}Seleccionar idioma{{else}}Select language{{/match}}
  {{/is}}
{{/match}}
{{#match key "SOCIAL_NET"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}Social_Net{{else has tag="#lang-pt-br"}}Social_Net{{else has tag="#lang-es-la"}}Social_Net{{else}}Social_Net{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}Social_Net{{else match (block "np_lang") "ja-jp"}}Social_Net{{else match (block "np_lang") "pt-br"}}Social_Net{{else match (block "np_lang") "es-la"}}Social_Net{{else}}Social_Net{{/match}}
  {{/is}}
{{/match}}
{{#match key "STATUS: AUTHENTICATED"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}STATUS: AUTHENTICATED{{else has tag="#lang-pt-br"}}STATUS: AUTHENTICATED{{else has tag="#lang-es-la"}}STATUS: AUTHENTICATED{{else}}STATUS: AUTHENTICATED{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}STATUS: AUTHENTICATED{{else match (block "np_lang") "ja-jp"}}STATUS: AUTHENTICATED{{else match (block "np_lang") "pt-br"}}STATUS: AUTHENTICATED{{else match (block "np_lang") "es-la"}}STATUS: AUTHENTICATED{{else}}STATUS: AUTHENTICATED{{/match}}
  {{/is}}
{{/match}}
{{#match key "STATUS_ONLINE"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}ステータス: オンライン{{else has tag="#lang-pt-br"}}STATUS: ONLINE{{else has tag="#lang-es-la"}}ESTADO: EN LÍNEA{{else}}STATUS: ONLINE{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}STATUS: ONLINE{{else match (block "np_lang") "ja-jp"}}ステータス: オンライン{{else match (block "np_lang") "pt-br"}}STATUS: ONLINE{{else match (block "np_lang") "es-la"}}ESTADO: EN LÍNEA{{else}}STATUS: ONLINE{{/match}}
  {{/is}}
{{/match}}
{{#match key "STATUS_ONLINE_ARCHIVE"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}ステータス: オンライン // アーカイブ索引{{else has tag="#lang-pt-br"}}STATUS: ONLINE // ÍNDICE DO ARQUIVO{{else has tag="#lang-es-la"}}ESTADO: EN LÍNEA // ÍNDICE DE ARCHIVO{{else}}STATUS: ONLINE // ARCHIVE INDEX{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}STATUS: ONLINE // ARCHIVE INDEX{{else match (block "np_lang") "ja-jp"}}ステータス: オンライン // アーカイブ索引{{else match (block "np_lang") "pt-br"}}STATUS: ONLINE // ÍNDICE DO ARQUIVO{{else match (block "np_lang") "es-la"}}ESTADO: EN LÍNEA // ÍNDICE DE ARCHIVO{{else}}STATUS: ONLINE // ARCHIVE INDEX{{/match}}
  {{/is}}
{{/match}}
{{#match key "STREAM_BUFFER_ACTIVE"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}STREAM_BUFFER_ACTIVE{{else has tag="#lang-pt-br"}}STREAM_BUFFER_ACTIVE{{else has tag="#lang-es-la"}}STREAM_BUFFER_ACTIVE{{else}}STREAM_BUFFER_ACTIVE{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}STREAM_BUFFER_ACTIVE{{else match (block "np_lang") "ja-jp"}}STREAM_BUFFER_ACTIVE{{else match (block "np_lang") "pt-br"}}STREAM_BUFFER_ACTIVE{{else match (block "np_lang") "es-la"}}STREAM_BUFFER_ACTIVE{{else}}STREAM_BUFFER_ACTIVE{{/match}}
  {{/is}}
{{/match}}
{{#match key "SYSTEM_READY"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}SYSTEM.READY{{else has tag="#lang-pt-br"}}SYSTEM.READY{{else has tag="#lang-es-la"}}SYSTEM.READY{{else}}SYSTEM.READY{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}SYSTEM.READY{{else match (block "np_lang") "ja-jp"}}SYSTEM.READY{{else match (block "np_lang") "pt-br"}}SYSTEM.READY{{else match (block "np_lang") "es-la"}}SYSTEM.READY{{else}}SYSTEM.READY{{/match}}
  {{/is}}
{{/match}}
{{#match key "SYSTEM_STATUS"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}SYSTEM_STATUS{{else has tag="#lang-pt-br"}}SYSTEM_STATUS{{else has tag="#lang-es-la"}}SYSTEM_STATUS{{else}}SYSTEM_STATUS{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}SYSTEM_STATUS{{else match (block "np_lang") "ja-jp"}}SYSTEM_STATUS{{else match (block "np_lang") "pt-br"}}SYSTEM_STATUS{{else match (block "np_lang") "es-la"}}SYSTEM_STATUS{{else}}SYSTEM_STATUS{{/match}}
  {{/is}}
{{/match}}
{{#match key "Search"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}SEARCH_SYS{{else has tag="#lang-pt-br"}}SEARCH_SYS{{else has tag="#lang-es-la"}}SEARCH_SYS{{else}}SEARCH_SYS{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}SEARCH_SYS{{else match (block "np_lang") "ja-jp"}}SEARCH_SYS{{else match (block "np_lang") "pt-br"}}SEARCH_SYS{{else match (block "np_lang") "es-la"}}SEARCH_SYS{{else}}SEARCH_SYS{{/match}}
  {{/is}}
{{/match}}
{{#match key "Share"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}Share{{else has tag="#lang-pt-br"}}Share{{else has tag="#lang-es-la"}}Share{{else}}Share{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}Share{{else match (block "np_lang") "ja-jp"}}Share{{else match (block "np_lang") "pt-br"}}Share{{else match (block "np_lang") "es-la"}}Share{{else}}Share{{/match}}
  {{/is}}
{{/match}}
{{#match key "Sign in"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}AUTHENTICATE_NODE{{else has tag="#lang-pt-br"}}AUTHENTICATE_NODE{{else has tag="#lang-es-la"}}AUTHENTICATE_NODE{{else}}AUTHENTICATE_NODE{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}AUTHENTICATE_NODE{{else match (block "np_lang") "ja-jp"}}AUTHENTICATE_NODE{{else match (block "np_lang") "pt-br"}}AUTHENTICATE_NODE{{else match (block "np_lang") "es-la"}}AUTHENTICATE_NODE{{else}}AUTHENTICATE_NODE{{/match}}
  {{/is}}
{{/match}}
{{#match key "Subscribe"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}SUBSCRIBE_TO_FEED{{else has tag="#lang-pt-br"}}SUBSCRIBE_TO_FEED{{else has tag="#lang-es-la"}}SUBSCRIBE_TO_FEED{{else}}SUBSCRIBE_TO_FEED{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}SUBSCRIBE_TO_FEED{{else match (block "np_lang") "ja-jp"}}SUBSCRIBE_TO_FEED{{else match (block "np_lang") "pt-br"}}SUBSCRIBE_TO_FEED{{else match (block "np_lang") "es-la"}}SUBSCRIBE_TO_FEED{{else}}SUBSCRIBE_TO_FEED{{/match}}
  {{/is}}
{{/match}}
{{#match key "TOKEN_DISPATCHED_TO_COMM_LINK"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}TOKEN_DISPATCHED_TO_COMM_LINK{{else has tag="#lang-pt-br"}}TOKEN_DISPATCHED_TO_COMM_LINK{{else has tag="#lang-es-la"}}TOKEN_DISPATCHED_TO_COMM_LINK{{else}}TOKEN_DISPATCHED_TO_COMM_LINK{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}TOKEN_DISPATCHED_TO_COMM_LINK{{else match (block "np_lang") "ja-jp"}}TOKEN_DISPATCHED_TO_COMM_LINK{{else match (block "np_lang") "pt-br"}}TOKEN_DISPATCHED_TO_COMM_LINK{{else match (block "np_lang") "es-la"}}TOKEN_DISPATCHED_TO_COMM_LINK{{else}}TOKEN_DISPATCHED_TO_COMM_LINK{{/match}}
  {{/is}}
{{/match}}
{{#match key "TRANSMITTING..."}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}TRANSMITTING...{{else has tag="#lang-pt-br"}}TRANSMITTING...{{else has tag="#lang-es-la"}}TRANSMITTING...{{else}}TRANSMITTING...{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}TRANSMITTING...{{else match (block "np_lang") "ja-jp"}}TRANSMITTING...{{else match (block "np_lang") "pt-br"}}TRANSMITTING...{{else match (block "np_lang") "es-la"}}TRANSMITTING...{{else}}TRANSMITTING...{{/match}}
  {{/is}}
{{/match}}
{{#match key "Theme"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}Theme{{else has tag="#lang-pt-br"}}Theme{{else has tag="#lang-es-la"}}Theme{{else}}Theme{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}Theme{{else match (block "np_lang") "ja-jp"}}Theme{{else match (block "np_lang") "pt-br"}}Theme{{else match (block "np_lang") "es-la"}}Theme{{else}}Theme{{/match}}
  {{/is}}
{{/match}}
{{#match key "Transmit"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}TRANSMIT{{else has tag="#lang-pt-br"}}TRANSMIT{{else has tag="#lang-es-la"}}TRANSMIT{{else}}TRANSMIT{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}TRANSMIT{{else match (block "np_lang") "ja-jp"}}TRANSMIT{{else match (block "np_lang") "pt-br"}}TRANSMIT{{else match (block "np_lang") "es-la"}}TRANSMIT{{else}}TRANSMIT{{/match}}
  {{/is}}
{{/match}}
{{#match key "UPLOADED"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}アップロード{{else has tag="#lang-pt-br"}}ENVIADO{{else has tag="#lang-es-la"}}SUBIDO{{else}}UPLOADED{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}UPLOADED{{else match (block "np_lang") "ja-jp"}}アップロード{{else match (block "np_lang") "pt-br"}}ENVIADO{{else match (block "np_lang") "es-la"}}SUBIDO{{else}}UPLOADED{{/match}}
  {{/is}}
{{/match}}
{{#match key "USER_PROFILE"}}
  {{#is "post"}}
    {{#post}}
      {{#has tag="#lang-ja-jp"}}ユーザープロフィール{{else has tag="#lang-pt-br"}}PERFIL_DO_USUÁRIO{{else has tag="#lang-es-la"}}PERFIL_DE_USUARIO{{else}}USER_PROFILE{{/has}}
    {{/post}}
  {{else}}
    {{#match (block "np_lang") "en-us"}}USER_PROFILE{{else match (block "np_lang") "ja-jp"}}ユーザープロフィール{{else match (block "np_lang") "pt-br"}}PERFIL_DO_USUÁRIO{{else match (block "np_lang") "es-la"}}PERFIL_DE_USUARIO{{else}}USER_PROFILE{{/match}}
  {{/is}}
{{/match}}
