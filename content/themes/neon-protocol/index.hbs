{{!< default}}

{{#contentFor "np_hreflang_kind"}}site{{/contentFor}}
{{#contentFor "np_hreflang_suffix"}}articles/{{/contentFor}}

<main class="np-main">
    <header class="np-about-banner">
        <h1 class="np-display"><span class="np-page-title"><span class="material-symbols-outlined" aria-hidden="true">database</span>{{> "np-t" key="ARTICLES_TITLE"}}</span></h1>
        <p class="np-meta">{{> "np-t" key="STATUS_ONLINE_ARCHIVE"}}</p>
    </header>

    <div class="np-toolbar">
        <div class="np-search">
            <span class="material-symbols-outlined" aria-hidden="true">search</span>
            <button type="button" data-ghost-search data-np-i18n-aria="Search" aria-label="{{> "np-t" key="Search"}}">{{> "np-t" key="Search"}}</button>
        </div>
        {{> "np-article-filters"}}
    </div>

    <div id="posts">
        {{> "np-articles-feed"}}
    </div>

    {{#unless posts}}
        <p class="np-empty np-meta">{{> "np-t" key="No posts"}}</p>
    {{/unless}}

    {{pagination}}
</main>
